package matano.apkode.net.matano.config;

/**
 * Created by smalllamartin on 1/1/17.
 */

public class Fake {
    /*
    TextUtils.isEmpty(myEditText) : check if EditTexit is empty
    Uri uri = Uri.fromFile(new File("")); get file from string


     Intent intent = new Intent(this, LoginActivity.class);
        intent.addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP);

     */
}
